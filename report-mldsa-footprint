#!/bin/bash

set -ex 

TOOLCHAIN=${1:-on/cortex-m33}
PRESET=${2:-minSizeRel}

TARGET=$(basename ${TOOLCHAIN})
OUTDIR="build/$TARGET"

./build-all-mldsa $TOOLCHAIN $PRESET ${@:3}

./report-footprint $OUTDIR
